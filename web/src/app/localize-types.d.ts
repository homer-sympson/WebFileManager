/**
 * Makes the global `$localize` function (used for messages authored in TypeScript) known to the
 * compiler. The runtime implementation is loaded through the `@angular/localize/init` polyfill
 * declared in `angular.json`.
 */
/// <reference types="@angular/localize" />
